<Project>
    <PropertyGroup>
        <!-- 定义 DLL 复制目标路径 -->
        <LibrariesPath>$(BaseOutputPath)Libraries\</LibrariesPath>
        <RuntimesPath>$(BaseOutputPath)Runtimes\</RuntimesPath>
    </PropertyGroup>
</Project>